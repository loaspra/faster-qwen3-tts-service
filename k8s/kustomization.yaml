apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

# CI rewrites this tag on every release.
images:
  - name: ghcr.io/loaspra/faster-qwen3-tts
    newTag: sha-2bd2f0c1
